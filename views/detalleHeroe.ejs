<!DOCTYPE html>
<html lang="en">

<%- include("./partials/head.ejs")  %> 
<body>
    <main>
        <h2>Detalle Héroe</h2>
        <section>
          
            <article class="<%= heroe.id %>">
               
                <h3><%= heroe.superhero %> </h3>
                <img src="/images/<%= heroe.id %>.jpg" alt="imagen de <%= heroe.superhero %>">
          
                <ul>
                    <li>
                        <% if (heroe.publisher === "DC Comics") { %>
                         <a href="/publisher/dc">  
                        <%= heroe.publisher %> 
                        </a>
                     
                        <% } else { %>
                            <a href="/publisher/marvel">  
                                <%= heroe.publisher %> 
                                <% } %>
                    </li>
                    <li><%= heroe.alter_ego %> </li>
                </ul>
            </article>
    
        </section>

    </main>
    
</body>
</html>